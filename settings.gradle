pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        repositories {
            maven { url 'https://jitpack.io' }
        }
        google()
        mavenCentral()
    }
}
rootProject.name = "DSAudioJetPack"
include ':app'
